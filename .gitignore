__pycache__/
*.pyc
.env
.env.*
!.env.example
.idea/
*.zip
node_modules/
.DS_Store
*.log
.playwright-mcp/
.claude/settings.local.json
# .claude/skills/ is intentionally tracked -- see CLAUDE.md, which documents these as
# part of the repo's own workflow (add-adapter, build-adapter-from-docs, etc.)

# Local scratch / sandbox checkouts (e.g. reference repos cloned for inspection)
tmp/

# Coverage artifacts from pytest --cov
.coverage
coverage.xml
htmlcov/

# pytest cache
.pytest_cache/
