# Development dependencies (linting, formatting, testing tools).
# Install runtime deps first: pip install -r requirements.txt
# Then dev:                  pip install -r requirements-dev.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
respx>=0.21.0  # mocks httpx at the transport layer -- exercises AssetHttpClient's REAL request()/
               # pagination code against realistic vendor-shaped responses, not just the adapter's
               # own methods. Standard part of the verification layer when no live account exists.

# Formatting & linting
black>=24.3.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.10.0

# Type stubs (for mypy)
types-requests
